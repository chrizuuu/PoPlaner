import { StyleSheet} from 'react-native';

const styleText = StyleSheet.create ({
   header: {
    fontSize:34,
    color:'#222222',
    fontWeight:'700',
    marginTop:20,
    opacity:0.8,
   },
   
   subHeader: {
    opacity:0.7,
    marginBottom:20,
    fontSize:16,
   },

});

export default styleText;